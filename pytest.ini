[pytest]
pythonpath = .
markers =
    integration: needs full data/splits and heavier subprocess checks
filterwarnings =
    ignore:Importing pandas-specific classes and functions from the:FutureWarning